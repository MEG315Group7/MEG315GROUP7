# Simplified Python Frontend Requirements for AD-HTC System
# These are the ONLY packages needed for the standalone Python frontend
# Install with: pip install -r requirements_python_frontend_simple.txt

# Core requirements (all are standard library or lightweight)
requests>=2.25.0  # For HTTP communication (only if using backend mode)

# Note: tkinter is part of Python standard library
# Note: All other dependencies have been removed to avoid conflicts
# The standalone frontend (adhtc_gui_simple.py) uses only standard library